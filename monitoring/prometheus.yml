global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # 추후 백엔드 애플리케이션(Spring Boot Actuator 등) 연동을 위한 예시
  # - job_name: 'backend-app'
  #   metrics_path: '/actuator/prometheus'
  #   static_configs:
  #     - targets: ['host.docker.internal:8080'] 